// Loop will iterate from 0 to 15.
for (let i = 0; i < 16; i++) {
    // Check if the current number is odd or even, and display a message in the console window.
    if (i % 2 == 0)
        document.write(i + ' is even<br>')
    else
        document.write(i + ' is odd<br>')
}